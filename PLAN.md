# 🏝️ Kite Safari Web - Детальный план MVP

> **Проект:** Веб-приложение для просмотра трипов и бронирований яхт
> **Репозиторий:** kitesafari-web (приватный)
> **Статус:** Plan Approved / Ready to Start
> **Дата создания:** 2025-01-14

---

## 📋 Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Функциональные требования](#функциональные-требования)
3. [Структура приложения](#структура-приложения)
4. [UI/UX спецификация](#uiux-спецификация)
5. [Технический стек](#технический-стек)
6. [Структура проекта](#структура-проекта)
7. [План реализации](#план-реализации)
8. [Deployment](#deployment)
9. [Чеклисты](#чеклисты)
10. [Фаза 2 и будущее](#фаза-2-и-будущее)

---

## 🎯 Обзор проекта

### Цель
Создать веб-приложение для просмотра информации о yacht трипах, яхтах и каютах с возможностью увидеть статус бронирований.

### Целевая аудитория

**Две категории пользователей:**

1. **Новички** - изучают трип впервые
   - Читают описание, даты, детали
   - Смотрят фото яхт
   - Решают, могут ли поехать

2. **Опытные** - были на трипах ранее
   - Быстро находят свободные каюты
   - Выбирают подходящую
   - Готовы бронировать

### Scope MVP

**✅ Включено в MVP:**
- Просмотр деталей трипа (название, даты, описание)
- Список яхт с фото и описаниями
- Просмотр всех кают со статусами
- Информация о забронировавших (имена/handles)
- Deck plans яхт
- Галереи фото яхт
- Адаптивный дизайн (mobile + desktop)

**❌ НЕ включено в MVP:**
- Бронирование через сайт (только через бота)
- Админ панель
- Пользовательский режим (скрытие имен)
- Фильтры и поиск
- Интерактивный deck plan
- Авторизация пользователей

### Подход к разработке

**Progressive Enhancement:**
1. **MVP v1:** Обычный веб-сайт (работает везде)
2. **MVP v2:** + Telegram Mini App интеграция
3. **v3:** + Бронирование через Mini App

---

## 🔧 Функциональные требования

### FR-1: Просмотр трипа

**Описание:** Пользователь может увидеть полную информацию о трипе

**Данные для отображения:**
- Название трипа (из `trips.name`)
- Даты (из `trips.start_date`, `trips.end_date`)
- Описание (из `trips.description`)
- Условия (из `trips.terms`)
- Фотографии трипа (из `trips.photos_urls[0]` - первое фото)
- Статистика: X из Y кают забронировано

**Acceptance Criteria:**
- ✅ При открытии URL с валидным access_code показывается информация о трипе
- ✅ При невалидном access_code показывается "Trip not found"
- ✅ Все текстовые поля корректно отображаются
- ✅ Фото загружаются (или placeholder если нет)

### FR-2: Список яхт

**Описание:** Отображение всех яхт участвующих в трипе

**Данные для отображения:**
- Название яхты (`yachts.name`)
- Описание (`yachts.description`)
- Одно фото (`yachts.photos_urls[0]`)
- Счетчик кают (свободно/всего)

**Acceptance Criteria:**
- ✅ Яхты отображаются списком на главной странице
- ✅ Для каждой яхты показано одно фото
- ✅ Показан счетчик доступных кают
- ✅ Есть кнопка/ссылка "Посмотреть каюты"

### FR-3: Просмотр кают

**Описание:** Детальный просмотр всех кают всех яхт

**Страница кают содержит для каждой яхты:**

1. **Deck Plan** - изображение поэтажного плана (`yachts.deck_plan_urls[0]`)
2. **Описание яхты** - повторяется из главной
3. **Галерея фото** - все фото яхты (`yachts.photos_urls`)
4. **Список кают** - структурированный список

**Структура списка кают:**
```
Yacht 1: Lyra
  ├─ Deck Plan (image)
  ├─ Description
  ├─ Photo Gallery
  └─ Cabins:
      ├─ Lower Deck (группа)
      │   ├─ Cabin 1: bed_type | price | status | guests
      │   ├─ Cabin 2: ...
      │   └─ Cabin 3: ...
      ├─ Main Deck (группа)
      │   └─ Cabin 4: ...
      └─ Upper Deck (группа)
          └─ Cabin 5: ...

Yacht 2: Andromeda
  └─ (та же структура)
```

**Данные каюты (в одной строке):**
- Номер каюты (`cabins.cabin_number`)
- Конфигурация кроватей (`cabins.bed_type`)
- Цена (`cabins.price`)
- Статус (`cabins.status`) - цветовой индикатор
- Гости (`cabins.guests`) - если забронировано

**Группировка и сортировка:**
- Группировка 1: По яхтам (`yachts.name`)
- Группировка 2: По декам (`cabins.deck`)
- Сортировка: По номеру каюты (возрастание)

**Acceptance Criteria:**
- ✅ Каюты сгруппированы по яхтам
- ✅ Внутри яхты каюты сгруппированы по декам
- ✅ Каюты отсортированы по номеру
- ✅ Deck plan отображается (или скрыт если нет URL)
- ✅ Галерея фото работает (carousel или grid)
- ✅ Все статусы корректно отображаются с цветами
- ✅ Имена гостей показываются as is

### FR-4: Статусы кают

**Возможные статусы:**

| Статус | Цвет | Иконка | Описание |
|--------|------|--------|----------|
| Available | Зеленый | 🟢 | Полностью свободна |
| Half Available | Желтый | 🟡 | Забронирована половина |
| Booked | Красный | 🔴 | Полностью забронирована |
| STAFF | Серый | ⚪ | Служебная каюта |

**Отображение гостей:**
- Если `status = Available` → гости не показываются
- Если `status = Booked/Half` → показать `cabins.guests` as is

**Примеры:**
```
🟢 Available (гости не показаны)
🔴 Booked: @fshubin Филипп Шубин, дочь Лея
🟡 Half Available: @GlebKhol Глеб Холодов
⚪ STAFF
```

---

## 🗺️ Структура приложения

### Страницы

**Всего 2 страницы:**

#### Страница 1: Trip Overview (главная)

**URL:** `/{accessCode}` (например `/egypt2025`)

**Разделы:**

1. **Hero Section**
   - Название трипа
   - Даты (formatted)
   - Главное описание

2. **Trip Details**
   - Полное описание
   - Условия (terms)
   - Статистика бронирований

3. **Yachts List**
   - Для каждой яхты:
     - Фото (первое из массива)
     - Название
     - Краткое описание
     - Счетчик кают (X available / Y total)

4. **Call to Action**
   - Кнопка "Посмотреть все каюты"

**Wireframe (текстовый):**
```
┌─────────────────────────────────────┐
│  🏝️ Египет Кайт Сафари             │
│  15 марта - 22 марта 2025           │
│  [Hero фото трипа]                  │
├─────────────────────────────────────┤
│  Описание трипа...                  │
│  Условия...                         │
│                                     │
│  📊 Статус: 12/24 кают забронировано│
├─────────────────────────────────────┤
│  🛥️ Яхты:                          │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ [Фото Lyra]                   │  │
│  │ Lyra                          │  │
│  │ Описание яхты...              │  │
│  │ 🟢 5 кают доступно из 12      │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ [Фото Andromeda]              │  │
│  │ Andromeda                     │  │
│  │ Описание...                   │  │
│  │ 🟢 3 каюты доступно из 12     │  │
│  └───────────────────────────────┘  │
├─────────────────────────────────────┤
│  [Посмотреть все каюты] (button)    │
└─────────────────────────────────────┘
```

#### Страница 2: Cabins View

**URL:** `/{accessCode}/cabins`

**Разделы (для каждой яхты):**

1. **Yacht Header**
   - Название яхты

2. **Deck Plan**
   - Изображение deck plan
   - (не интерактивный в MVP)

3. **Yacht Description**
   - Полное описание яхты

4. **Photo Gallery**
   - Все фото яхты
   - Carousel или Grid

5. **Cabins List**
   - Группировка по декам
   - Таблица/список кают

**Wireframe (текстовый):**
```
┌─────────────────────────────────────┐
│ ← Назад к трипу                     │
├─────────────────────────────────────┤
│ 🛥️ Lyra                             │
│                                     │
│ [Deck Plan Image]                   │
│                                     │
│ Описание яхты...                    │
│                                     │
│ Галерея: [фото1][фото2][фото3]...   │
│                                     │
│ ━━━ Lower Deck ━━━                  │
│ #1 │ 2 beds │ 2800€ │ 🟢 Available │
│ #2 │ 2 beds │ 2800€ │ 🔴 @user...  │
│ #3 │ Double │ 2800€ │ 🟡 @ivan     │
│                                     │
│ ━━━ Main Deck ━━━                   │
│ #4 │ 2 beds │ 3000€ │ 🟢 Available │
│ #5 │ Double │ 3200€ │ ⚪ STAFF      │
│                                     │
├─────────────────────────────────────┤
│ 🛥️ Andromeda                        │
│ (та же структура)                   │
└─────────────────────────────────────┘
```

### Навигация

**Navigation Flow:**
```
/{accessCode}
    ↓ [Посмотреть каюты]
/{accessCode}/cabins
    ↑ [← Назад]
/{accessCode}
```

**Breadcrumbs:** Не требуются (только 2 уровня)

**Navigation Bar:**
- Логотип/название (link на главную)
- Название трипа (всегда видно)

---

## 🎨 UI/UX спецификация

### Дизайн система

**Цветовая схема:**
- Основной фон: `#FFFFFF` (белый)
- Текст: `#000000` / `#333333` (черный/темно-серый)
- Акценты:
  - Success/Available: `#22C55E` (зеленый)
  - Warning/Half: `#EAB308` (желтый)
  - Error/Booked: `#EF4444` (красный)
  - Info/STAFF: `#94A3B8` (серый)

**Типографика:**
- Шрифт: Системный (`system-ui, -apple-system, sans-serif`)
- Размеры:
  - H1 (Trip name): `32px` / `2rem`
  - H2 (Yacht name): `24px` / `1.5rem`
  - H3 (Deck name): `18px` / `1.125rem`
  - Body: `16px` / `1rem`
  - Small: `14px` / `0.875rem`

**Отступы (spacing):**
- XS: `4px`
- S: `8px`
- M: `16px`
- L: `24px`
- XL: `32px`
- XXL: `48px`

**Border Radius:**
- Cards: `8px`
- Buttons: `6px`
- Images: `4px`

### Компоненты

#### YachtCard (на главной странице)

```
┌─────────────────────────────┐
│ [Фото яхты 400x300]         │
│                             │
│ Lyra                        │ (H2)
│ Описание яхты...            │ (Body)
│                             │
│ 🟢 5 доступно / 12 всего    │ (Small)
└─────────────────────────────┘
```

**CSS структура:**
```css
.yacht-card {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 16px;
}

.yacht-card__image {
  width: 100%;
  height: 300px;
  object-fit: cover;
  border-radius: 4px;
}

.yacht-card__name {
  font-size: 24px;
  font-weight: 600;
  margin: 16px 0 8px;
}

.yacht-card__description {
  color: #666;
  margin-bottom: 12px;
}

.yacht-card__stats {
  font-size: 14px;
  color: #22C55E; /* зеленый если есть свободные */
}
```

#### CabinRow (строка каюты)

**Desktop:**
```
| #1 | 2 separate beds | 2800 EUR | 🟢 Available |
```

**Mobile (стек):**
```
#1
2 separate beds
2800 EUR
🟢 Available
```

**CSS структура:**
```css
.cabin-row {
  display: grid;
  grid-template-columns: 60px 1fr 120px 200px;
  gap: 16px;
  padding: 12px;
  border-bottom: 1px solid #e5e7eb;
  align-items: center;
}

@media (max-width: 768px) {
  .cabin-row {
    grid-template-columns: 1fr;
    gap: 8px;
  }
}

.cabin-row__number {
  font-weight: 600;
  font-size: 18px;
}

.cabin-row__status {
  display: flex;
  align-items: center;
  gap: 8px;
}

.status-badge {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.status-badge--available { background: #22C55E; }
.status-badge--booked { background: #EF4444; }
.status-badge--half { background: #EAB308; }
.status-badge--staff { background: #94A3B8; }
```

#### PhotoGallery

**Вариант: Horizontal Scroll (mobile-friendly)**

```
┌─────────────────────────────────────┐
│ [Photo 1] [Photo 2] [Photo 3] →     │
└─────────────────────────────────────┘
```

**CSS:**
```css
.photo-gallery {
  display: flex;
  gap: 12px;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  padding: 16px 0;
}

.photo-gallery__item {
  flex-shrink: 0;
  width: 300px;
  height: 200px;
  scroll-snap-align: start;
}

.photo-gallery__item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 8px;
}
```

#### DeckPlan

```css
.deck-plan {
  max-width: 100%;
  height: auto;
  margin: 24px 0;
}

.deck-plan img {
  width: 100%;
  height: auto;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
}
```

### Адаптивность (Responsive)

**Breakpoints:**
- Mobile: `< 768px`
- Tablet: `768px - 1024px`
- Desktop: `> 1024px`

**Mobile-first подход:**
- Базовые стили для мобильных
- Media queries для desktop улучшений

**Приоритет:** Мобильная версия (т.к. в будущем Telegram Mini App)

### Loading States

**Пока данные загружаются:**
```tsx
<div className="loading">
  Загрузка трипа...
</div>
```

**Skeleton screens** (опционально в будущем):
- Placeholder блоки вместо контента

### Error States

**Trip not found:**
```
┌─────────────────────────┐
│   ❌ Trip not found     │
│                         │
│   Invalid access code   │
└─────────────────────────┘
```

**Ошибка загрузки:**
```
┌─────────────────────────┐
│   ⚠️ Error loading      │
│                         │
│   [Retry]               │
└─────────────────────────┘
```

---

## 💻 Технический стек

### Frontend

**Core:**
- **React 18** - UI library
- **TypeScript** - Type safety
- **Vite** - Build tool & dev server

**Routing:**
- **React Router v6** - Client-side routing

**Data:**
- **Supabase JS SDK** - Database client (прямой доступ)
- **Native fetch** - HTTP requests (если нужно)

**Styling:**
- **CSS Modules** - Scoped styles
- **Системные шрифты** - Нет внешних шрифтов

**Development:**
- **ESLint** - Linting
- **Prettier** - Code formatting (опционально)

### Backend

**База данных:**
- **Supabase** (PostgreSQL)
- **Row Level Security** для защиты

**Таблицы (существующие):**
- `trips` - информация о трипах
- `yachts` - яхты
- `cabins` - каюты

**Доступ:**
- **Read-only** через ANON_KEY
- **Публичные RLS политики** для чтения

### Hosting & Deployment

**Хостинг:**
- **GitHub Pages** - Static site hosting
- Бесплатно для приватных репозиториев

**Domain:**
- Временно: `username.github.io/kitesafari-web`
- В будущем: Custom domain

**CI/CD:**
- GitHub Actions для автоматического деплоя

### Environment Variables

**Development (`.env.local`):**
```env
VITE_SUPABASE_URL=https://zmbiiywazaytltemzzvc.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
```

**Production:**
- Переменные в GitHub Secrets
- Используются при build

---

## 📁 Структура проекта

```
kitesafari-web/
├── .github/
│   └── workflows/
│       └── deploy.yml           # GitHub Actions для деплоя
├── public/
│   ├── favicon.ico
│   └── placeholder.jpg          # Placeholder для отсутствующих фото
├── src/
│   ├── components/
│   │   ├── Layout/
│   │   │   ├── Layout.tsx
│   │   │   └── Layout.module.css
│   │   ├── YachtCard/
│   │   │   ├── YachtCard.tsx
│   │   │   └── YachtCard.module.css
│   │   ├── CabinRow/
│   │   │   ├── CabinRow.tsx
│   │   │   └── CabinRow.module.css
│   │   ├── PhotoGallery/
│   │   │   ├── PhotoGallery.tsx
│   │   │   └── PhotoGallery.module.css
│   │   ├── DeckPlan/
│   │   │   ├── DeckPlan.tsx
│   │   │   └── DeckPlan.module.css
│   │   ├── StatusBadge/
│   │   │   ├── StatusBadge.tsx
│   │   │   └── StatusBadge.module.css
│   │   └── ErrorBoundary/
│   │       └── ErrorBoundary.tsx
│   ├── pages/
│   │   ├── TripPage/
│   │   │   ├── TripPage.tsx
│   │   │   └── TripPage.module.css
│   │   ├── CabinsPage/
│   │   │   ├── CabinsPage.tsx
│   │   │   └── CabinsPage.module.css
│   │   └── NotFoundPage/
│   │       ├── NotFoundPage.tsx
│   │       └── NotFoundPage.module.css
│   ├── lib/
│   │   ├── supabase.ts          # Supabase client init
│   │   └── utils.ts             # Helper functions
│   ├── hooks/
│   │   ├── useTrip.ts           # Fetch trip data
│   │   ├── useYachts.ts         # Fetch yachts
│   │   └── useCabins.ts         # Fetch cabins
│   ├── types/
│   │   └── index.ts             # TypeScript types
│   ├── App.tsx                  # Root component
│   ├── App.css                  # Global styles
│   ├── main.tsx                 # Entry point
│   └── vite-env.d.ts
├── .env.example                 # Template для .env
├── .gitignore
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── README.md                    # Документация проекта
└── PLAN.md                      # Этот файл

```

**Принципы организации:**
- **Components:** Каждый компонент в своей папке с CSS модулем
- **Pages:** Страницы как top-level компоненты
- **Lib:** Утилиты и инициализация библиотек
- **Hooks:** Custom React hooks для data fetching
- **Types:** TypeScript определения

---

## 🚀 План реализации

### Фаза 1: Setup проекта (0.5 дня)

**Задачи:**

1. **Создать репозиторий**
   ```bash
   # На GitHub
   Create new private repository: kitesafari-web

   # Локально
   cd /Users/evgenyq/Projects/kitesafari-web
   git init
   ```

2. **Инициализировать Vite проект**
   ```bash
   npm create vite@latest . -- --template react-ts
   npm install
   ```

3. **Установить зависимости**
   ```bash
   npm install react-router-dom @supabase/supabase-js
   ```

4. **Настроить конфигурацию**
   - `vite.config.ts` - base URL для GitHub Pages
   - `tsconfig.json` - path aliases (если нужно)
   - `.env.example` - template для переменных

5. **Настроить Supabase**
   - Создать `src/lib/supabase.ts`
   - Инициализировать клиент
   - Протестировать подключение

6. **Базовая структура папок**
   - Создать папки: components, pages, hooks, lib, types

**Результат:** Работающий dev server с подключением к Supabase

---

### Фаза 2: TypeScript типы и hooks (0.5 дня)

**Задачи:**

1. **Определить TypeScript типы**

```typescript
// src/types/index.ts

export interface Trip {
  id: string
  notion_id: string
  name: string
  start_date: string
  end_date: string
  access_code: string
  description: string | null
  terms: string | null
  photos_urls: string[] | null
  status: string
}

export interface Yacht {
  id: string
  notion_id: string
  name: string
  description: string | null
  photos_urls: string[] | null
  deck_plan_urls: string[] | null
}

export interface Cabin {
  id: string
  notion_id: string
  trip_id: string
  yacht_id: string
  cabin_number: number
  deck: string
  bed_type: string
  price: number
  status: CabinStatus
  guests: string | null
  max_guests: number
}

export type CabinStatus = 'Available' | 'Booked' | 'Half Available' | 'STAFF'
```

2. **Создать data fetching hooks**

```typescript
// src/hooks/useTrip.ts
export function useTrip(accessCode: string) {
  // Fetch trip by access_code
  // Return { data, loading, error }
}

// src/hooks/useYachts.ts
export function useYachts(tripId: string) {
  // Fetch yachts for trip
  // Join with cabins to count available
}

// src/hooks/useCabins.ts
export function useCabins(tripId: string) {
  // Fetch all cabins with yacht info
  // Group by yacht_id and deck
}
```

**Результат:** Типизированные данные и хуки для их получения

---

### Фаза 3: Страница Trip Overview (1 день)

**Задачи:**

1. **Создать Layout компонент**
   - Header с названием
   - Main content area
   - Footer (опционально)

2. **Создать TripPage**
   - Hero section (название, даты)
   - Description section
   - Stats section (X/Y кают)

3. **Создать YachtCard компонент**
   - Фото яхты
   - Название, описание
   - Счетчик кают

4. **Интеграция с данными**
   - useTrip hook
   - useYachts hook
   - Loading/Error states

5. **Стилизация**
   - CSS Modules для всех компонентов
   - Responsive design
   - Mobile-first

**Acceptance Criteria:**
- ✅ Открывается URL `/{accessCode}`
- ✅ Показывается информация о трипе
- ✅ Список яхт отображается корректно
- ✅ Фото загружаются или placeholder
- ✅ Работает на мобильных и desktop
- ✅ Loading state при загрузке
- ✅ Error state если трип не найден

**Результат:** Работающая главная страница

---

### Фаза 4: Страница Cabins (1.5 дня)

**Задачи:**

1. **Создать CabinsPage структуру**
   - Layout для списка яхт
   - Навигация назад

2. **Создать YachtSection компонент**
   - Header с названием яхты
   - DeckPlan компонент
   - Description
   - PhotoGallery компонент
   - CabinsList компонент

3. **Создать DeckPlan компонент**
   - Показать изображение deck plan
   - Responsive sizing
   - Placeholder если нет

4. **Создать PhotoGallery компонент**
   - Horizontal scroll
   - Touch-friendly
   - Lazy loading (опционально)

5. **Создать CabinRow компонент**
   - Grid layout (desktop)
   - Stack layout (mobile)
   - StatusBadge integration

6. **Создать StatusBadge компонент**
   - Цветовой индикатор
   - Текст статуса
   - Гости (если есть)

7. **Группировка и сортировка**
   - Group cabins by yacht
   - Group by deck within yacht
   - Sort by cabin_number

8. **Интеграция данных**
   - useCabins hook
   - Обработка группировки
   - Loading/Error states

**Acceptance Criteria:**
- ✅ Открывается URL `/{accessCode}/cabins`
- ✅ Для каждой яхты показан deck plan
- ✅ Галерея фото работает (scroll/swipe)
- ✅ Каюты сгруппированы по декам
- ✅ Каюты отсортированы по номеру
- ✅ Все статусы отображаются корректно
- ✅ Имена гостей показываются
- ✅ Responsive design

**Результат:** Полностью работающая страница кают

---

### Фаза 5: Навигация и UX (0.5 дня)

**Задачи:**

1. **Настроить React Router**
   ```tsx
   <Routes>
     <Route path="/:accessCode" element={<TripPage />} />
     <Route path="/:accessCode/cabins" element={<CabinsPage />} />
     <Route path="*" element={<NotFoundPage />} />
   </Routes>
   ```

2. **Добавить навигацию**
   - Кнопка "Посмотреть каюты" на TripPage
   - Кнопка "Назад" на CabinsPage
   - Логотип/название -> home

3. **404 страница**
   - NotFoundPage component
   - Сообщение "Trip not found"

4. **UX улучшения**
   - Smooth scroll
   - Focus states
   - Transitions (опционально)

**Результат:** Полная навигация между страницами

---

### Фаза 6: Supabase RLS и безопасность (0.5 дня)

**Задачи:**

1. **Настроить Row Level Security**

```sql
-- Enable RLS
ALTER TABLE trips ENABLE ROW LEVEL SECURITY;
ALTER TABLE yachts ENABLE ROW LEVEL SECURITY;
ALTER TABLE cabins ENABLE ROW LEVEL SECURITY;

-- Public read policies
CREATE POLICY "Public read trips"
ON trips FOR SELECT
USING (true);

CREATE POLICY "Public read yachts"
ON yachts FOR SELECT
USING (true);

CREATE POLICY "Public read cabins"
ON cabins FOR SELECT
USING (true);

-- Deny all writes for anon
CREATE POLICY "Deny writes"
ON trips FOR INSERT
USING (false);
-- (аналогично для UPDATE, DELETE на всех таблицах)
```

2. **Тестирование доступа**
   - Проверить что ANON_KEY может читать
   - Проверить что ANON_KEY НЕ может писать

**Результат:** Безопасный read-only доступ к БД

---

### Фаза 7: Тестирование и багфиксы (0.5 дня)

**Задачи:**

1. **Функциональное тестирование**
   - Проверить все страницы
   - Проверить все статусы кают
   - Проверить edge cases (нет фото, нет deck plan)

2. **Responsive тестирование**
   - iPhone (Safari)
   - Android (Chrome)
   - Desktop (Chrome, Firefox, Safari)

3. **Performance check**
   - Lighthouse audit
   - Размер bundle (<500kb желательно)
   - Время загрузки (<3s)

4. **Багфиксы**
   - Исправить найденные баги
   - Улучшить UX где нужно

**Результат:** Стабильное приложение без критичных багов

---

### Фаза 8: Deployment (0.5 дня)

**Задачи:**

1. **Подготовить production build**
   ```bash
   npm run build
   # Проверить dist/ папку
   ```

2. **Настроить GitHub Pages**

   **vite.config.ts:**
   ```ts
   export default defineConfig({
     base: '/kitesafari-web/',
     // ...
   })
   ```

3. **Создать GitHub Action**

```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

4. **Настроить GitHub Secrets**
   - Settings → Secrets → New repository secret
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

5. **Активировать GitHub Pages**
   - Settings → Pages
   - Source: gh-pages branch
   - Save

6. **Первый деплой**
   ```bash
   git add .
   git commit -m "Initial deployment"
   git push origin main
   ```

7. **Проверить production**
   - Открыть `https://username.github.io/kitesafari-web/egypt2025`
   - Проверить что все работает

**Результат:** Приложение задеплоено и доступно онлайн

---

## 📊 Timeline

| Фаза | Задача | Время |
|------|--------|-------|
| 1 | Setup проекта | 0.5 дня |
| 2 | TypeScript типы и hooks | 0.5 дня |
| 3 | Страница Trip Overview | 1 день |
| 4 | Страница Cabins | 1.5 дня |
| 5 | Навигация и UX | 0.5 дня |
| 6 | Supabase RLS | 0.5 дня |
| 7 | Тестирование | 0.5 дня |
| 8 | Deployment | 0.5 дня |
| **ИТОГО** | **MVP v1** | **5-6 дней** |

---

## 🔐 Deployment

### GitHub Pages Setup

**Репозиторий:**
- Private repository на GitHub
- Название: `kitesafari-web`

**URL:**
- Development: `http://localhost:5173`
- Production: `https://[username].github.io/kitesafari-web/`

**Environment Variables:**

В GitHub Secrets добавить:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

**Build command:**
```bash
npm run build
```

**Deploy:**
- Автоматически через GitHub Actions при push в main

**Custom Domain (в будущем):**
- Настроить CNAME
- Добавить в GitHub Pages settings

---

## ✅ Чеклисты

### Pre-launch Checklist

**Функционал:**
- [ ] Главная страница отображает trip info
- [ ] Список яхт показывается
- [ ] Страница кают работает
- [ ] Все статусы кают отображаются корректно
- [ ] Deck plans показываются
- [ ] Фото галереи работают
- [ ] Навигация между страницами работает
- [ ] 404 страница для невалидных access codes

**UI/UX:**
- [ ] Responsive на мобильных (iPhone, Android)
- [ ] Responsive на планшетах
- [ ] Responsive на desktop
- [ ] Все фото загружаются или показывают placeholder
- [ ] Loading states показываются
- [ ] Error states обрабатываются
- [ ] Нет горизонтального скролла (кроме галерей)

**Performance:**
- [ ] Lighthouse Score > 80
- [ ] Bundle size < 500KB
- [ ] First Contentful Paint < 2s
- [ ] Time to Interactive < 3s

**Безопасность:**
- [ ] RLS политики настроены
- [ ] ANON_KEY не дает write доступ
- [ ] Environment variables в Secrets (не в коде)
- [ ] Нет hardcoded credentials

**Code Quality:**
- [ ] TypeScript без errors
- [ ] ESLint без warnings (критичных)
- [ ] Все компоненты типизированы
- [ ] CSS Modules используются
- [ ] Нет console.log в production

**Deployment:**
- [ ] GitHub Actions работает
- [ ] Production build успешен
- [ ] Сайт доступен по URL
- [ ] Environment variables применяются
- [ ] Нет 404 ошибок на ресурсы

### Testing Checklist

**Manual Testing:**

**Trip Page:**
- [ ] URL `/{accessCode}` открывается
- [ ] Trip name, dates отображаются
- [ ] Description показывается
- [ ] Terms показываются (если есть)
- [ ] Фото трипа загружается
- [ ] Статистика кают корректна
- [ ] Yachts list показывается
- [ ] Yacht photos загружаются
- [ ] Счетчик кают правильный
- [ ] Кнопка "Посмотреть каюты" работает

**Cabins Page:**
- [ ] URL `/{accessCode}/cabins` открывается
- [ ] Для каждой яхты:
  - [ ] Deck plan показывается
  - [ ] Description показывается
  - [ ] Photo gallery работает
  - [ ] Cabins list отображается
  - [ ] Группировка по декам работает
  - [ ] Сортировка по номерам работает
- [ ] Статусы:
  - [ ] 🟢 Available отображается
  - [ ] 🔴 Booked отображается
  - [ ] 🟡 Half Available отображается
  - [ ] ⚪ STAFF отображается
- [ ] Guests показываются (если есть)
- [ ] Кнопка "Назад" работает

**Edge Cases:**
- [ ] Невалидный access_code → 404
- [ ] Trip без yachts → сообщение
- [ ] Yacht без deck plan → скрыт
- [ ] Yacht без фото → placeholder
- [ ] Cabin без guests → не показываются

**Cross-browser:**
- [ ] Chrome (desktop)
- [ ] Firefox (desktop)
- [ ] Safari (desktop)
- [ ] Safari (iOS)
- [ ] Chrome (Android)

**Responsive:**
- [ ] iPhone SE (375px)
- [ ] iPhone 14 (390px)
- [ ] iPad (768px)
- [ ] Desktop (1920px)

---

## 🔮 Фаза 2 и будущее

### MVP v2: Telegram Mini App интеграция

**Добавить:**
- `npm install @twa-dev/sdk`
- Platform detection (`src/lib/platform.ts`)
- Telegram WebApp initialization
- Theme adaptation (Telegram colors)
- User info display (если в Telegram)

**Timeline:** +1-2 дня

### Фаза 3: Бронирование

**Добавить:**
- Booking flow в Mini App
- Form для деталей гостя
- Integration с ботом для финализации
- Payment info

**Timeline:** +3-4 дня

### Фаза 4: Пользовательский режим

**Добавить:**
- Авторизация (через Telegram)
- Два режима: Admin / User
- Скрытие имен в User режиме
- Admin panel для управления

**Timeline:** +2-3 дня

### Будущие фичи (nice to have)

- [ ] Интерактивный deck plan (кликабельные каюты)
- [ ] Фильтры и поиск кают
- [ ] Сравнение кают (side-by-side)
- [ ] PWA support (offline mode)
- [ ] Push notifications
- [ ] Export в PDF/Calendar
- [ ] История изменений
- [ ] Analytics (какие каюты популярны)

---

## 📚 Справочная информация

### Полезные команды

```bash
# Development
npm run dev              # Запустить dev server
npm run build           # Production build
npm run preview         # Preview production build
npm run lint            # Run ESLint

# Git
git add .
git commit -m "feat: add trip page"
git push origin main

# Deployment (автоматически через GitHub Actions)
# или вручную:
npm run build
# затем deploy dist/ folder
```

### Структура URL

```
Production:
https://[username].github.io/kitesafari-web/egypt2025
https://[username].github.io/kitesafari-web/egypt2025/cabins

Development:
http://localhost:5173/egypt2025
http://localhost:5173/egypt2025/cabins
```

### Контакты и ресурсы

**Документация:**
- React: https://react.dev
- Vite: https://vitejs.dev
- React Router: https://reactrouter.com
- Supabase: https://supabase.com/docs
- GitHub Pages: https://pages.github.com

**Связанные проекты:**
- Bot: `kitesafaribot` (Python Telegram bot)
- Database: Supabase (shared)

---

## 📝 Changelog

**2025-01-14:**
- Создан initial plan
- Определены функциональные требования
- Спроектирована структура приложения
- Составлен timeline
- Plan approved ✅

---

## ✅ Status

**Current Status:** ✅ Plan Approved - Ready to Start

**Next Steps:**
- [ ] Создать GitHub репозиторий
- [ ] Начать Фазу 1: Setup проекта
- [ ] Обновить этот документ по мере прогресса

---

**Конец документа**
